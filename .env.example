# Local development defaults. Copy to .env.local only when overrides are needed.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_ANON_KEY=<run-supabase-status-and-copy-the-local-anon-key>

# Production values belong in the Vercel environment configuration and must
# never be committed to this repository.
